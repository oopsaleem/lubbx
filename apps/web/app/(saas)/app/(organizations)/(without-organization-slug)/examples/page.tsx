import { SidebarRtl } from "@saas/organizations/components/SidebarRtl";

export default async function SidebarRtlPage() {
    // const locale = useLocale();
	// const dir = locale === "ar" ? "rtl" : "ltr";
    
    return (
					// <div className="relative" dir={dir}>
						<SidebarRtl />
					// </div>
				);
}